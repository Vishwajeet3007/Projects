# AI Code Review Bot VS Code Extension

Use the extension commands from the Command Palette:

- `AI Code Review: Review Current File`
- `AI Code Review: Review Repository URL`
- `AI Code Review: Review Pull Request URL`

Set `aiCodeReview.apiBaseUrl` if your backend is not running on `http://localhost:8000/api/v1`.
